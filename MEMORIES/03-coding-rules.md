# Memory: Coding Rules

- Revisa `src/lib/db/schema.ts` antes de implementar queries o modelos.
- Aplica scoping por `orgId`/`userId` cuando corresponda.
- Evita `any` y `console.log` en producción.
- Ejecuta `npm run lint` antes del build.
